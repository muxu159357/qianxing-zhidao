---
description: 黔行智导项目多端开发规则 — 小程序端 + Web 端双端协同
alwaysApply: true
---

# 黔行智导：贵州山地旅游 AI 伴游与安全守护平台

本项目是 2026 贵州省人工智能创业大赛参赛项目，采用"双端协同"策略：小程序端是游客核心产品，Web 端是项目展示+运营管理+路演展示端。

## 产品外显原则（CRITICAL）

用户端页面必须面向真实游客，**严禁出现以下词汇**：
Demo / demo / 测试 / 比赛 / 参赛 / 评委 / 路演 / 演示流程 / 占位页 / 开发中 / Mock / 假数据 / 临时页面 / TODO / test / coming soon

这些词只允许出现在内部开发文档和规则文件中。

功能暂未完成时的产品化表达：
- "正在为你整理更完整的行程建议"
- "更多路线能力即将开放"
- "安全提醒功能已开启基础模式"

## 赛题对齐

严格围绕赛题三「围绕山地旅游游客兴趣建模与个性化导览」，必须体现四个核心方向：

1. **游客兴趣建模与个性化导览** — 识别兴趣、人群、体力偏好，生成个性化推荐
2. **山地动态路径规划与实时重规划** — 体力消耗、天气、拥堵模拟 + 替代方案
3. **游客异常行为识别与风险预警** — 偏离路线、长时间停留、体力下降、高风险区域
4. **游客求助与安全辅助** — 附近服务点、医疗点、救援点、紧急联系

## 评审导向

- **创新性**：体现 AI、画像、推荐、重规划、安全预警，不做普通旅游网站
- **技术难度**：清晰算法逻辑、风险评估规则、评分机制
- **完成度**：小程序走通完整流程（首页→兴趣→画像→推荐→详情→伴游→安全→行程）
- **应用价值**：贴近贵州山地旅游真实痛点

## 双端定位

### 小程序端（游客主产品 · 优先开发）
首页、兴趣选择、游客画像、路线推荐、路线详情、AI山地伴游、安全守护、景点知识库、景点详情、我的行程
- 微信原生、移动优先、按钮≥44px、文案面向真实游客

### Web 端（项目展示 + 运营管理）
项目官网、运营管理、景点管理、路线管理、画像洞察、AI问答记录、安全预警记录、数据看板
- 不删除已有页面、游客流程页面保留为体验版、重点转向后台管理

## 双端共享
景点/路线/兴趣标签/用户画像/AI问答/安全提醒 数据字段保持同步，后续后端接口同时服务双端。

## 接口预留

| 功能 | 接口 |
|------|------|
| 兴趣标签 | `GET /api/interest-tags` |
| 生成画像 | `POST /api/visitor-profile/generate` |
| 路线推荐 | `POST /api/route-recommendations` |
| 动态重规划 | `POST /api/route-replan` |
| 景点列表 | `GET /api/scenic-spots` |
| 景点详情 | `GET /api/scenic-spots/{id}` |
| AI 问答 | `POST /api/ai-guide/chat` |
| 安全提醒 | `GET /api/safety-alerts` |
| 附近服务点 | `GET /api/nearby-services` |
| 异常行为上报 | `POST /api/safety/behavior-check` |
| 我的行程 | `GET/POST/DELETE /api/user-trips` |
| 后台景点管理 | `CRUD /api/admin/scenic-spots` |
| 后台路线管理 | `CRUD /api/admin/routes` |
| 数据看板 | `GET /api/admin/dashboard/stats` |
| 安全预警记录 | `GET /api/admin/safety-records` |

## 开发前判断

每次开发计划必须包含：

> **所属端：** Web 端 / 小程序端 / 双端共享能力
> **划分理由：** （一句话）
> **涉及文件：** （列表）
> **是否涉及用户可见文案：** 是 / 否
> **是否需要清理违禁词汇：** 是 / 否
> **贴合赛题方向：** 方向一/二/三/四
> **后端接口预留：** （对应API）

判断标准：游客使用 → 小程序优先 / 后台管理+数据统计+路演 → Web 优先 / 两端都需要 → 先统一数据结构

## UI 设计铁律

- **GSAP 强制**：所有页面入场/滚动/过渡动画必须用 GSAP，禁止 CSS @keyframes 作为主要动画
- **图片优先**：真实图片 > SVG 图标 > 纯文字，禁止 Emoji 图标（🧭🏞️🤖❌）
- **找不到图就找用户要**：先搜索 Unsplash/Pixabay/Pexels，找不到就列出缺失清单
- **美观标准**：大留白、字重对比（700+/400）、色彩克制（三色：#1f8f5f #2f6bff #e67e22），按钮 ≥ 44px

## 禁止事项

1. 用户端不出现 Demo/测试/比赛/开发中/占位等词
2. 不做普通旅游攻略 — 必须体现 AI 伴游 + 安全守护
3. 不只做路线推荐 — 必须逐步加入安全守护
4. 不删除 Web 端已有页面
5. 不破坏小程序主流程
6. 不引入复杂依赖（未确认时）
7. 小程序启动页不一次性加载大量数据
8. 不把 Web 写法带入小程序
9. 不为炫技牺牲可运行性
10. 不偏离贵州山地旅游场景
11. 不使用 Emoji 代替图标或景点图
12. 不写没有动画的静态页面

## 当前优先级

1. 清理用户端违禁词汇
2. 小程序独立路线详情页
3. 小程序安全守护能力
4. 小程序视觉优化
5. 小程序知识库扩充
6. Web 端运营管理能力
7. 技术文档 + 测试报告 + 演示视频脚本

## 项目进度记录

每次任务完成后必须更新 `PROJECT_PROGRESS.md`，按固定格式输出：当前进度、本次完成、修改文件、已实现/待优化/未实现、问题风险、下一步建议（≤3条）、验证清单。下次任务前先汇报当前进度。

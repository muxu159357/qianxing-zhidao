---
description: 黔行智导项目多端开发规则 — Web 端 + 微信小程序端双端协同
alwaysApply: true
---

# 黔行智导 — 多端开发规则

本项目采用"双端协同"策略：小程序端是游客核心产品，Web 端是项目展示+后台管理+路演展示端。

## 小程序端定位

面向游客真实使用，优先完成：首页 → 兴趣选择 → 画像生成 → 路线推荐 → 路线详情 → AI 导游 → 知识库 → 景点详情 → 我的行程。

- 使用微信原生小程序结构，优先适配手机端
- 页面交互简洁顺手，适合游客边走边用
- 不接真实后端前，使用本地 Mock 数据
- 适合录制比赛演示视频

## Web 端定位

不删除已有 Web 页面，继续保留并逐步完善：项目展示官网、路演展示页、后台管理、数据统计看板、景点管理、路线管理。

- 不再作为游客端主入口
- 已有游客流程页面可保留为演示版
- 后续重点转向后台管理和路演展示
- 风格保持科技感+文旅感+贵州地域特色

## 双端共享原则

- 景点/路线/兴趣标签/用户画像/AI 问答数据字段保持同步
- Mock 数据可各端单独存放，但字段设计保持一致
- 后续后端接口同时服务 Web 和小程序

## 接口预留

| 功能 | 接口 |
|------|------|
| 兴趣标签 | `GET /api/interest-tags` |
| 生成画像 | `POST /api/visitor-profile/generate` |
| 路线推荐 | `POST /api/route-recommendations` |
| 景点列表 | `GET /api/scenic-spots` |
| 景点详情 | `GET /api/scenic-spots/{id}` |
| AI 问答 | `POST /api/ai-guide/chat` |
| 我的行程 | `GET /api/user-trips` |
| 后台景点管理 | `CRUD /api/admin/scenic-spots` |
| 后台路线管理 | `CRUD /api/admin/routes` |
| 数据看板 | `GET /api/admin/dashboard/stats` |

## 开发前判断

每次开发计划必须包含"所属端"一栏，格式：

> **所属端：** Web 端 / 小程序端 / 双端共享能力
> **划分理由：** （一句话说明为什么）

判断标准：
- 游客使用功能 → 小程序端优先
- 后台管理/数据统计/路演 → Web 端优先
- 两端都需要 → 先统一数据结构，再分别实现

## UI 设计铁律

- **GSAP 强制**：所有页面入场/滚动/过渡动画必须用 GSAP，禁止 CSS @keyframes 作为主要动画
- **图片优先**：真实图片 > SVG 图标 > 纯文字，禁止 Emoji 图标（🧭🏞️🤖❌）
- **找不到图就找用户要**：先搜索 Unsplash/Pixabay/Pexels，找不到就列出缺失清单向用户索要
- **美观标准**：大留白、字重对比、色彩克制（三色），移动端按钮 ≥ 44px，图片高清

## 禁止事项

1. 不把小程序做成 Web 后台风格
2. 不删除 Web 端已有页面
3. 不让两端数据字段各写各的
4. 不强行混在一起复用代码
5. 不一边开发小程序一边破坏 Web 已有成果
6. 不引入 uni-app/Taro/Pinia/后端接口/地图 SDK/真实 AI API（未确认时）
7. 不忘记演示目标：小程序展示游客体验，Web 展示项目能力和后台价值
8. **不使用 Emoji 代替图标或景点图**
9. **不写没有动画的静态页面**

## 当前优先级

1. 微信小程序游客端 Demo 闭环
2. Web 端项目展示和后台管理
3. 后端接口设计
4. 真实 AI / RAG / 地图接入

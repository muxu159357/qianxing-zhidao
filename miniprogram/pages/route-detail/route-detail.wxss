/* 路线详情页 */
.page-wrap {
  min-height: 100vh;
  background: #f4f8f5;
  padding-bottom: 160rpx;
}

/* 空态 */
.empty-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 80vh;
  padding: 48rpx;
}
.empty-card {
  text-align: center;
  padding: 64rpx 48rpx;
  border-radius: 28rpx;
  background: rgba(255,255,255,0.85);
  box-shadow: 0 16rpx 48rpx rgba(19,61,45,0.06);
  max-width: 600rpx;
  width: 100%;
}
.empty-icon {
  width: 80rpx; height: 80rpx;
  border: 5rpx solid #1f8f5f;
  border-radius: 50%;
  margin: 0 auto 24rpx;
  position: relative;
  background: rgba(31,143,95,0.06);
}
.empty-icon::before {
  content: '';
  position: absolute; top: 50%; left: 50%;
  width: 0; height: 0;
  border-left: 8rpx solid transparent;
  border-right: 8rpx solid transparent;
  border-bottom: 16rpx solid #1f8f5f;
  transform: translate(-50%, -60%) rotate(-90deg);
}
.empty-icon::after {
  content: '';
  position: absolute; top: 50%; left: 50%;
  width: 12rpx; height: 12rpx;
  border-radius: 50%;
  background: #1f8f5f;
  transform: translate(4rpx, 6rpx);
}
.empty-title { font-size: 36rpx; font-weight: 700; color: #10251d; margin-bottom: 16rpx; }
.empty-desc { font-size: 28rpx; color: #5d6b7a; line-height: 1.7; margin-bottom: 40rpx; }

/* 路线头部 */
.route-hero {
  padding: 48rpx 40rpx 40rpx;
  background: linear-gradient(135deg, #1f8f5f 0%, #2f6bff 100%);
  color: #fff;
}
.route-hero-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
}
.route-hero-main { flex: 1; }
.route-name {
  font-size: 44rpx;
  font-weight: 800;
  letter-spacing: 0.02em;
  margin-bottom: 16rpx;
}
.route-desc {
  font-size: 28rpx;
  line-height: 1.7;
  opacity: 0.9;
}
.route-score {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 28rpx;
}
.score-circle {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.22);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}
.score-num { font-size: 36rpx; font-weight: 800; }
.score-unit { font-size: 20rpx; opacity: 0.85; }
.score-label { font-size: 26rpx; opacity: 0.85; }

/* 基本信息 */
.info-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
  padding: 32rpx 32rpx 16rpx;
}
.info-item {
  padding: 24rpx;
  border-radius: 16rpx;
  background: rgba(255,255,255,0.85);
  box-shadow: 0 4rpx 16rpx rgba(19,61,45,0.04);
}
.info-label { font-size: 22rpx; color: #8c9ba6; margin-bottom: 8rpx; }
.info-value { font-size: 28rpx; font-weight: 700; color: #10251d; }
.info-value-sm { font-size: 24rpx; }

/* 标签行 */
.tag-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  padding: 0 32rpx 24rpx;
}
.tag-item {
  font-size: 22rpx;
  padding: 8rpx 20rpx;
  border-radius: 999rpx;
  background: rgba(31,143,95,0.08);
  color: #1f8f5f;
  font-weight: 600;
}

/* 分区 */
.section {
  padding: 32rpx 32rpx 0;
}
.section-title {
  font-size: 34rpx;
  font-weight: 800;
  color: #10251d;
  margin-bottom: 20rpx;
  padding-left: 16rpx;
  border-left: 6rpx solid #1f8f5f;
}
.safety-title {
  border-left-color: #e67e22;
}

/* 时间线 */
.timeline { padding: 0; }
.tl-day { display: flex; gap: 16rpx; padding-bottom: 8rpx; }
.tl-marker {
  width: 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}
.tl-dot {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: #1f8f5f;
  margin-top: 8rpx;
}
.tl-line {
  width: 3rpx;
  flex: 1;
  background: rgba(31,143,95,0.18);
  margin-top: 8rpx;
}
.tl-content {
  flex: 1;
  padding: 12rpx 20rpx 24rpx;
  margin-bottom: 8rpx;
  border-radius: 16rpx;
  background: rgba(255,255,255,0.85);
  box-shadow: 0 4rpx 16rpx rgba(19,61,45,0.04);
}
.tl-head { font-size: 28rpx; font-weight: 700; color: #1f5f45; margin-bottom: 8rpx; }
.tl-desc { font-size: 26rpx; color: #4d5f6f; line-height: 1.65; margin-bottom: 10rpx; }
.tl-meta { font-size: 22rpx; color: #909399; }

/* 景点卡片 */
.scenic-grid { display: flex; flex-direction: column; gap: 16rpx; }
.scenic-card {
  display: flex;
  align-items: center;
  border-radius: 16rpx;
  overflow: hidden;
  background: rgba(255,255,255,0.85);
  box-shadow: 0 4rpx 16rpx rgba(19,61,45,0.04);
}
.scenic-gradient {
  width: 100rpx;
  height: 100rpx;
  flex-shrink: 0;
  border-radius: 12rpx;
}
.scenic-info {
  flex: 1;
  padding: 16rpx 20rpx;
}
.scenic-name { font-size: 28rpx; font-weight: 700; color: #10251d; }
.scenic-city { font-size: 22rpx; color: #8c9ba6; margin-top: 4rpx; }
.scenic-rating { font-size: 22rpx; color: #e67e22; margin-top: 4rpx; font-weight: 600; }
.scenic-arrow {
  padding: 0 24rpx;
  font-size: 28rpx;
  color: #c0c4cc;
}

/* 贴士卡片 */
.tip-card {
  padding: 24rpx 28rpx;
  border-radius: 16rpx;
  background: rgba(255,255,255,0.85);
  box-shadow: 0 4rpx 16rpx rgba(19,61,45,0.04);
}
.tip-text {
  font-size: 26rpx;
  color: #4d5f6f;
  line-height: 1.75;
  margin-bottom: 12rpx;
}
.tip-text:last-child { margin-bottom: 0; }
.tip-label { font-weight: 700; color: #10251d; }

/* 安全守护卡片 */
.safety-card {
  padding: 28rpx;
  border-radius: 20rpx;
  background: rgba(255,255,255,0.9);
  border: 1rpx solid rgba(230,126,34,0.15);
  box-shadow: 0 8rpx 32rpx rgba(230,126,34,0.06);
}
.safety-row {
  display: flex;
  align-items: flex-start;
  gap: 12rpx;
  padding: 14rpx 0;
}
.safety-row:first-child { padding-top: 0; }
.safety-label { font-size: 26rpx; font-weight: 700; color: #10251d; }
.safety-icon {
  flex-shrink: 0; width: 36rpx; height: 36rpx;
  display: flex; align-items: center; justify-content: center;
  position: relative;
}
/* 风险 — 三角警示 */
.safety-icon-risk::before {
  content: '';
  width: 0; height: 0;
  border-left: 10rpx solid transparent;
  border-right: 10rpx solid transparent;
  border-bottom: 20rpx solid #e67e22;
}
/* 天气 — 圆形太阳 */
.safety-icon-weather::before {
  content: '';
  width: 22rpx; height: 22rpx;
  border-radius: 50%;
  background: #f59e0b;
}
/* 山地 — 双三角山峰 */
.safety-icon-mountain::before {
  content: '';
  width: 0; height: 0;
  border-left: 10rpx solid transparent;
  border-right: 10rpx solid transparent;
  border-bottom: 18rpx solid #1f8f5f;
}
.safety-icon-mountain::after {
  content: '';
  position: absolute; top: 10rpx; right: 4rpx;
  width: 0; height: 0;
  border-left: 7rpx solid transparent;
  border-right: 7rpx solid transparent;
  border-bottom: 12rpx solid #2f6bff;
}
/* 错峰/人群 — 双圆点 */
.safety-icon-crowd::before {
  content: '';
  width: 10rpx; height: 10rpx;
  border-radius: 50%;
  background: #2f6bff;
  box-shadow: 15rpx 0 0 0 #2f6bff;
}
/* 时间 — 圆形时钟 */
.safety-icon-time::before {
  content: '';
  width: 24rpx; height: 24rpx;
  border-radius: 50%;
  border: 3rpx solid #909399;
  box-sizing: border-box;
}
.safety-icon-time::after {
  content: '';
  position: absolute; top: 6rpx; left: 50%;
  width: 2rpx; height: 8rpx;
  background: #909399;
  transform: translateX(-50%);
}
.safety-text { font-size: 26rpx; color: #4d5f6f; line-height: 1.65; flex: 1; }

.risk-badge {
  font-size: 24rpx;
  padding: 6rpx 20rpx;
  border-radius: 999rpx;
  font-weight: 700;
}
.risk-low { background: rgba(31,143,95,0.1); color: #1f8f5f; }
.risk-mid { background: rgba(230,126,34,0.1); color: #e67e22; }
.risk-high { background: rgba(239,68,68,0.1); color: #ef4444; }

.safety-divider {
  height: 1rpx;
  background: #eef2f5;
  margin: 20rpx 0;
}

.safety-subtitle {
  font-size: 28rpx;
  font-weight: 700;
  color: #10251d;
  margin-bottom: 14rpx;
}

.service-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 24rpx;
}
.service-item {
  font-size: 24rpx;
  padding: 8rpx 18rpx;
  border-radius: 8rpx;
  background: rgba(47,107,255,0.06);
  color: #2f6bff;
  font-weight: 600;
}

.emergency-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 8rpx;
}
.emergency-item {
  flex: 1;
  min-width: 160rpx;
  padding: 20rpx 16rpx;
  border-radius: 14rpx;
  background: rgba(239,68,68,0.04);
  text-align: center;
}
.emergency-label { font-size: 22rpx; color: #8c9ba6; margin-bottom: 6rpx; }
.emergency-number { font-size: 32rpx; font-weight: 800; color: #ef4444; letter-spacing: 0.04em; }
.emergency-note { font-size: 22rpx; color: #8c9ba6; }

/* 替代路线 */
.alt-route-box {
  margin-top: 24rpx;
  padding: 28rpx;
  border-radius: 16rpx;
  background: rgba(31,143,95,0.04);
  border: 1rpx solid rgba(31,143,95,0.12);
}
.alt-route-label { font-size: 24rpx; color: #1f8f5f; font-weight: 600; margin-bottom: 10rpx; }
.alt-route-name { font-size: 30rpx; font-weight: 700; color: #10251d; margin-bottom: 6rpx; }
.alt-route-level { font-size: 24rpx; color: #5d6b7a; margin-bottom: 20rpx; }

/* 底部操作栏 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  gap: 20rpx;
  padding: 24rpx 32rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  background: rgba(255,255,255,0.92);
  backdrop-filter: blur(20rpx);
  border-top: 1rpx solid rgba(31,143,95,0.08);
  z-index: 10;
}
.btn-save {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 44rpx;
  font-size: 30rpx;
  font-weight: 700;
  background: rgba(31,143,95,0.08);
  color: #1f8f5f;
  border: 2rpx solid rgba(31,143,95,0.2);
}
.btn-guide {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 44rpx;
  font-size: 30rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #1f8f5f, #2f6bff);
  color: #fff;
  border: none;
}

/* 通用按钮 */
.btn-primary {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 44rpx;
  font-size: 30rpx;
  font-weight: 700;
  background: linear-gradient(135deg, #1f8f5f, #2f6bff);
  color: #fff;
  border: none;
}
.btn-outline-sm {
  width: 100%;
  height: 72rpx;
  line-height: 72rpx;
  border-radius: 36rpx;
  font-size: 28rpx;
  font-weight: 600;
  background: transparent;
  color: #1f8f5f;
  border: 2rpx solid rgba(31,143,95,0.3);
}

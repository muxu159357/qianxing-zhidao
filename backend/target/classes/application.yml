spring:
  profiles:
    active: dev

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

jwt:
  secret: ${JWT_SECRET:}
  expiration-ms: 86400000

wx:
  appid: ${WX_APPID:}
  secret: ${WX_SECRET:}
